// next.config.ts: Next.js Konfiguration (hier leer, aber für Anpassungen nutzbar)
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
